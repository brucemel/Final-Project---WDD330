<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('partials/head') %>
</head>
<body>
  <%- include('partials/header', { currentPage: 'favorites' }) %>
  <main class="favorites-main">
    <div class="favorites-header">
      <div>
        <h1 class="favorites-title">Your Favorites</h1>
        <p class="favorites-subtitle">Quotes you've saved for inspiration</p>
      </div>
      <button class="btn btn-danger" id="clearAllBtn" aria-label="Clear all favorites">
        Clear All
      </button>
    </div>
    <div class="search-wrapper favorites-search">
      <label for="favSearchInput" class="sr-only">Search favorites</label>
      <input type="search" id="favSearchInput" class="search-input"
        placeholder="Search your favorites by quote or author..." aria-label="Search favorites">
    </div>
    <div class="empty-state" id="emptyState" hidden>
      <div class="empty-icon">♡</div>
      <h2>No favorites yet</h2>
      <p>Head back to the home page and save some quotes you love!</p>
      <a href="/" class="btn btn-new">Go to Home</a>
    </div>
    <div class="favorites-grid" id="favoritesGrid" aria-label="Saved favorite quotes"></div>
  </main>
  <!-- Music Player -->
  <div class="music-player" id="musicPlayer" aria-label="Background music player">
    <div class="music-info">
      <span class="music-title" id="musicTitle">Loading music...</span>
      <span class="music-artist" id="musicArtist"></span>
    </div>
    <div class="music-controls">
      <button class="music-btn" id="musicPrevBtn" aria-label="Previous track">&#9664;&#9664;</button>
      <button class="music-btn music-btn-play" id="musicPlayBtn" aria-label="Play music">&#9654;</button>
      <button class="music-btn" id="musicNextBtn" aria-label="Next track">&#9654;&#9654;</button>
    </div>
    <input type="range" class="music-volume" id="musicVolume"
      min="0" max="1" step="0.05" aria-label="Volume">
  </div>

  <div class="toast" id="toast" role="alert" aria-live="polite"></div>
  <%- include('partials/footer') %>
  <script type="module" src="/js/favorites.js"></script>
</body>
</html>